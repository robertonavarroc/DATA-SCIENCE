> # CODIGOS DE PYTHON
- Codigos utilizados para trabajar con python
- :eye: Cuando instales python siempre utlizar una version estable, no la ultima, adicional siempre marcar la opcion **`Path`**.

```
python --version
``` 
- Para ver la version de python, ir a cmd y colocar el codigo **`"python --version"`**.

```
python
```
- Para ingresar al modo consola de python, debemos con cmd abrir el simbolo de sistema, y escribimos python.

```
quit()
```
- Utilizamos este codigo dentro de la consola de python para salir.

<BR>

---
<BR>
<BR>

> # EJECUTAR PYTHON EN VSCODE
- Se tiene que mover a la carpeta donde esta el archivo de python
    - DATA-SCIENCE/Clases/dia3 
    - Una vez situado en la ruta se ejecuta el comando
    - > python `nombre_archivo.py`
        - Comando se utiliza para ejecutar las instrucciones de python 

- Metodo 2 es utliza la opcion de `run code` o `CTRL+ALT+N`
    - > :arrow_forward: Icono que se encuentra a la mano derecha de las pestañas de codigo